
enum AuthenticatedStatus {authenticated, unauthenticated}

enum LoginFormStatus { unknown,  error, success }

enum SignUpStatus { unknown, busy, error, success }

enum ForgotPasswordStatus { unknown, busy, error, success }
